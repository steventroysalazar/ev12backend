server:
  port: 8090

spring:
 datasource:
        url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://ep-fancy-pond-a17fqnwa-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require}
        driver-class-name: ${SPRING_DATASOURCE_DRIVER_CLASS_NAME:org.postgresql.Driver}
        username: ${SPRING_DATASOURCE_USERNAME:neondb_owner}
        password: ${SPRING_DATASOURCE_PASSWORD:npg_kdvM23qafLQn}

 jpa:
        hibernate:
            ddl-auto: update
        open-in-view: false

 h2:
    console:
      enabled: ${SPRING_H2_CONSOLE_ENABLED:true}
      path: /h2-console

gateway:
  base-url: http://192.168.1.37:8082
  token: "00f30633-4029-4cb7-b088-1f84e7251a53"
  default-limit: 100

webhook:
  ev12-token: ${WEBHOOK_EV12_TOKEN:}
  ev12-persist-events: ${WEBHOOK_EV12_PERSIST_EVENTS:false}
  ev12-in-memory-events-max: ${WEBHOOK_EV12_IN_MEMORY_EVENTS_MAX:1000}

cisco:
  control-center:
    enabled: ${CISCO_CC_ENABLED:false}
    host: ${CISCO_CC_HOST:}
    username: ${CISCO_CC_USERNAME:}
    api-key: ${CISCO_CC_API_KEY:}
    device-details-path: ${CISCO_CC_DEVICE_DETAILS_PATH:/rws/api/v1/devices/{iccid}}
    activate-path: ${CISCO_CC_ACTIVATE_PATH:/rws/api/v1/devices/{iccid}/activate}
    deactivate-path: ${CISCO_CC_DEACTIVATE_PATH:/rws/api/v1/devices/{iccid}/deactivate}
    timeout-ms: ${CISCO_CC_TIMEOUT_MS:30000}
